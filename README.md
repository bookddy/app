# Bookddy

## running frontend

To run it in hot reload mode and for dev:

```
    npx next dev 
```

To run it in production:
```
    npx next start
```

To build the bundle 

```
    npx next build
```